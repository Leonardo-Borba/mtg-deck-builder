import { CardQuantityDirective } from './card-quantity.directive';

describe('CardQuantityDirective', () => {
  it('should create an instance', () => {
    const directive = new CardQuantityDirective();
    expect(directive).toBeTruthy();
  });
});
